pub fn main() -> i64 {
    let x = 10;

    let y = if x > 5 {
        x * 2
    } else {
        x / 2
    };

    let z = {
        let tmp = y + 3;
        tmp * tmp
    };

    x = if z > 100 {
        z - 100
    } else {
        z + 100
    };

    x + y + z
}
